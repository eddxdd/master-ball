"""Phase 7's win-probability toy model — real feature engineering
(app/ml/features.py) over real seeded Pokedex data, trained on a documented
SYNTHETIC battle-outcome simulator (app/ml/simulate.py) since no dataset of
real logged ladder match results is available to this project. See
app/tools/win_probability.py's module docstring and Docs/backend/README.md's
"Win probability model (Phase 7)" section for the full, upfront caveat before
using this module's output for anything beyond a portfolio demonstration of
the ML pipeline (feature engineering -> XGBoost -> served model).
"""
